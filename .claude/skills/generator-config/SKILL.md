---
name: generator-config
description: Create or review outputs/generator-config.md for a competitive-programming problem. Use for workflow Step 5 to turn constraints, subtasks, and solution weaknesses into the design contract that must be completed before test-script.txt and gentest.cpp.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/generator-config/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/generator-config/`. Do not edit anything under `.claude/skills/generator-config/` — apply
changes to the `.agents` copy instead.
