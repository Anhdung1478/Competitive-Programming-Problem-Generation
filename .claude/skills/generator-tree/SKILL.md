---
name: generator-tree
description: Generate adversarial and diverse tree structures for gentest.cpp. Use whenever the problem input is a tree, rooted tree, parent array, or a graph subtask constrained to be a tree.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/generator-tree/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/generator-tree/`. Do not edit anything under `.claude/skills/generator-tree/` — apply
changes to the `.agents` copy instead.
