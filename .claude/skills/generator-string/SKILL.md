---
name: generator-string
description: Generate strings and string-pattern adversarial cases for gentest.cpp. Use when input contains strings, alphabets, substrings, matching, borders, palindromes, periodicity, runs, or lexicographic constraints.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/generator-string/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/generator-string/`. Do not edit anything under `.claude/skills/generator-string/` — apply
changes to the `.agents` copy instead.
