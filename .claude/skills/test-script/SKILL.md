---
name: test-script
description: Create or review outputs/test-script.txt for Codeforces Polygon. Use for workflow Step 6 after outputs/generator-config.md is complete, deriving the generated test set, subtask/rate/seed options, and adversarial profile coverage from that contract.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/test-script/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/test-script/`. Do not edit anything under `.claude/skills/test-script/` — apply
changes to the `.agents` copy instead.
