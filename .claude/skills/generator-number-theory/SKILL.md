---
name: generator-number-theory
description: Generate number-theoretic values for gentest.cpp. Use when primes, divisors, multiples, gcd/lcm, factorization, prime powers, modular structure, or arithmetic edge cases are central to the problem.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/generator-number-theory/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/generator-number-theory/`. Do not edit anything under `.claude/skills/generator-number-theory/` — apply
changes to the `.agents` copy instead.
