---
name: generator-graph
description: Generate general graph structures for gentest.cpp. Use for directed/undirected graphs, simple graphs, multigraphs, connectivity constraints, DAGs, sparse/dense cases, or graph-specific subtasks.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/generator-graph/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/generator-graph/`. Do not edit anything under `.claude/skills/generator-graph/` — apply
changes to the `.agents` copy instead.
