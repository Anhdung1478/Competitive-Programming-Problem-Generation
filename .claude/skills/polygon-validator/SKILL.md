---
name: polygon-validator
description: Create or review outputs/validator.cpp for a Codeforces Polygon problem using testlib.h. Use for optional workflow Step 8 (skipped unless the user asks for it), generated-input validation, strict input-format enforcement, bounds checks, subtask/group constraints, or structural invariant validation. This validates input files, not contestant output.
---

This skill is a reference. The canonical, complete instructions live in the shared
`.agents` skill directory so a single skill is maintained in one place only.

**Read `.agents/skills/polygon-validator/SKILL.md` now and follow it in full.**

Resolve every relative path in that file (for example `references/...`) against
`.agents/skills/polygon-validator/`. Do not edit anything under `.claude/skills/polygon-validator/` — apply
changes to the `.agents` copy instead.
